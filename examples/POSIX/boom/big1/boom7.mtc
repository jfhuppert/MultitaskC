/*
   boom.mtc
*/

#include "boom.h"

int main()
{
   execute {
      execute { TASK(0); }
      and {
         execute { TASK(1); }
         and { TASK(2); }
      }
   }
   and {
      execute {
         execute { TASK(3); }
         and { TASK(4); }
      }
      and {
         execute { TASK(5); }
         and { TASK(6); }
      }
   }
}
