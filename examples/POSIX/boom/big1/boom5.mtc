/*
   boom.mtc
*/

#include "boom.h"

int main()
{
   execute {
      execute { TASK(0); }
      and { TASK(1); }
   }
   and {
      execute { TASK(2); }
      and {
         execute { TASK(3); }
         and { TASK(4); }
      }
   }
}
